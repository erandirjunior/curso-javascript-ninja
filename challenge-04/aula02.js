// Condicional Ternário -> uma outra forma de fazer o if. Formato do ternário: condição ? true : false;

1 === 2 ? true : false; //false

if (1 === 2) { true } else { false } //false

var pessoa = { sexo: 'Masculino' }; //undefined

var sexo = pessoa.sexo === 'Feminino' ? 'a' : 'o'; //undefined

sexo; //'o'

1 ? true : false; //true

var carro = 'string' ? 'porta' : 'janela'; //undefined

carro; //'porta'
